# Research Agent LLM Wiki

This wiki is the governed memory layer for the long-running research-agent workshop.

It contains reviewed or staged research artifacts. Raw tool outputs belong in
`raw/`; durable research pages belong in `wiki/`.

Agents must route through `INDEX.md`, preserve source IDs, and stage uncertain claims for review.
